force-file-write: true
formatter: goimports
log-level: info

structname: '{{.Mock}}{{.InterfaceName}}'
pkgname: 'mock{{.SrcPackageName}}'

template: testify
template-schema: '{{.Template}}.schema.json'

packages:
  go.sriracha.dev:
    config:
      all: true
      recursive: true
      dir: 'mock/{{.SrcPackageName}}'
      filename: "mocks.go"